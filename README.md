
  # Movie Streaming Website UI

  This is a code bundle for Movie Streaming Website UI. The original project is available at https://www.figma.com/design/Xg34HIOd90mrAmPhSbcUxm/Movie-Streaming-Website-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  